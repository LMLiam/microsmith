apply plugin: 'org.jetbrains.kotlin.kapt'

dependencies {
    api project(":compile-schemas")
    api project(":artifact-schemas-protobuf")
    api libs.spi.tooling.annotations
    kapt libs.spi.tooling.processor
}
